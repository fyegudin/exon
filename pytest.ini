# This is a basic structure, and you can expand it based on your specific needs. The conftest.py file contains fixtures,
# and in this example, we created a database fixture that provides a new Database instance for each test function.

[pytest]
python_files = test_*.py

